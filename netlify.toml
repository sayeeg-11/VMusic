# Netlify Configuration
[build]
  command = "npm run build"
  publish = "dist"

# Environment variables are set in Netlify Dashboard
# Site Settings → Environment Variables

# Secrets Scanning Configuration
[build.environment]
  # Skip scanning source code and build output
  # - dist/: Vite bundles VITE_* env vars (public by design)
  # - src/: Contains fallback URLs for local development
  # - docs/: Documentation with example configurations
  SECRETS_SCAN_OMIT_PATHS = "dist/**,src/**,docs/**,.env.example,SECURITY_FIX_COMPLETE.md"
  
  # Alternative: Disable secrets scanning entirely (not recommended)
  # SECRETS_SCAN_ENABLED = "false"

# Headers for security
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"

# Redirect rules
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
